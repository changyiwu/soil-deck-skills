name: Validate soil-teaching-deck

on:
  push:
    paths:
      - "skills/soil-teaching-deck/**"
      - "scripts/validate_all.py"
      - "requirements-dev.txt"
      - ".github/workflows/validate-soil-teaching-deck.yml"
  pull_request:
    paths:
      - "skills/soil-teaching-deck/**"
      - "scripts/validate_all.py"
      - "requirements-dev.txt"
      - ".github/workflows/validate-soil-teaching-deck.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -r requirements-dev.txt
      - run: python scripts/validate_all.py --target teaching
