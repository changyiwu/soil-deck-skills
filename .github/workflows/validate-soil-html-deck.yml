name: Validate soil-html-deck

on:
  push:
    paths:
      - "skills/soil-html-deck/**"
      - ".github/workflows/validate-soil-html-deck.yml"
  pull_request:
    paths:
      - "skills/soil-html-deck/**"
      - ".github/workflows/validate-soil-html-deck.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install PyYAML Pillow
      - run: >-
          python skills/soil-html-deck/scripts/validate_interactive_spec.py
          --spec skills/soil-html-deck/assets/interactive-spec-template.yaml
